# atuin のデフォルト設定からの差分のみを書く。書いていないキーは atuin 側の
# デフォルトのまま（`atuin config` に全キー入りのテンプレートが生成されるが、
# それを丸ごと追跡すると差分が埋もれるのでここには書かない）。

# fzf/skim系のスコアリング・ハイライトに寄せる
search_mode = "skim"

# フルスクリーン占有をやめて、画面下部に最大15行だけ表示する
style = "compact"
inline_height = 15
