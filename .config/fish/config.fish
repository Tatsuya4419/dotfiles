if status is-interactive
    abbr -a g git
    abbr -a c clear
    abbr -a gc git checkout
    abbr -a gs git status
    abbr -a ga git add
    abbr -a gb git branch
    abbr -a gp git push
    abbr -a gl git log
    abbr -a gf git fetch
    abbr -a xr xargs -I {}
    abbr -a ll ls -alg
    abbr -a tr tree -L 2
end
